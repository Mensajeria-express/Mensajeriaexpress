import React, { useState } from 'react';
import { 
  Package, 
  MapPin, 
  Truck, 
  ChevronLeft, 
  ChevronRight,
  Star,
  User,
  Bike
} from 'lucide-react';

const Testimonial = ({ text, author }) => (
  <div className="bg-white/80 p-4 rounded-xl backdrop-blur-sm">
    <p className="text-gray-700 text-sm">{text}</p>
    <div className="flex items-center mt-2">
      <div className="flex">
        {[...Array(5)].map((_, i) => (
          <Star key={i} size={12} className="text-yellow-400 fill-yellow-400" />
        ))}
      </div>
      <p className="text-sm font-medium ml-2">{author}</p>
    </div>
  </div>
);

const MensajeriaExpressApp = () => {
  const [currentScreen, setCurrentScreen] = useState('onboarding');
  const [currentTestimonial, setCurrentTestimonial] = useState(0);

  const testimonials = [
    {
      text: "Mensajería Express revolucionó mis envíos. ¡Rápido y confiable!",
      author: "María G."
    },
    {
      text: "Seguimiento en tiempo real que realmente funciona.",
      author: "Carlos R."
    },
    {
      text: "La mejor app de envíos que he usado hasta ahora.",
      author: "Andrea M."
    }
  ];

  return (
    <div className="h-screen relative overflow-hidden" style={{
      background: `linear-gradient(to bottom right, #2f4196, #d0e3ff)`
    }}>
      {/* Patrones de fondo */}
      <div className="absolute inset-0 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAwIiBoZWlnaHQ9IjIwMCIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cGF0aCBkPSJNMTAwIDBoMTAwdjEwMEgxMDBWMHoiIGZpbGw9InJnYmEoMjU1LDI1NSwyNTUsMC4wNSkiLz48L3N2Zz4=')] opacity-10" />
      
      <div className="max-w-md mx-auto h-full flex flex-col justify-between p-8">
        {/* Logo y encabezado */}
        <div className="text-center space-y-6 mt-12">
          <div className="relative">
            <div className="absolute inset-0 blur-2xl bg-white/20 rounded-full" />
            <div className="relative bg-white/90 w-24 h-24 mx-auto rounded-full flex items-center justify-center shadow-lg">
              <Package size={48} className="text-[#2f4196]" />
            </div>
          </div>
          
          <div className="space-y-4">
            <h1 className="text-4xl font-bold text-white" style={{ fontFamily: 'Poppins, sans-serif' }}>
              Mensajería Express
            </h1>
            <p className="text-lg text-white/90">
              Tus envíos en tiempo real, sin complicaciones
            </p>
          </div>
        </div>

        {/* Iconos ilustrativos */}
        <div className="flex justify-center space-x-8 my-8">
          <div className="text-white/90 text-center">
            <div className="bg-white/20 p-4 rounded-xl backdrop-blur-sm">
              <Bike size={32} />
            </div>
            <p className="text-sm mt-2">Mensajeros</p>
          </div>
          <div className="text-white/90 text-center">
            <div className="bg-white/20 p-4 rounded-xl backdrop-blur-sm">
              <User size={32} />
            </div>
            <p className="text-sm mt-2">Clientes</p>
          </div>
        </div>

        {/* Testimoniales */}
        <div className="relative my-8">
          <button 
            onClick={() => setCurrentTestimonial((curr) => (curr - 1 + testimonials.length) % testimonials.length)}
            className="absolute left-0 top-1/2 -translate-y-1/2 bg-white/20 p-2 rounded-full backdrop-blur-sm"
          >
            <ChevronLeft size={20} className="text-white" />
          </button>
          
          <button 
            onClick={() => setCurrentTestimonial((curr) => (curr + 1) % testimonials.length)}
            className="absolute right-0 top-1/2 -translate-y-1/2 bg-white/20 p-2 rounded-full backdrop-blur-sm"
          >
            <ChevronRight size={20} className="text-white" />
          </button>
          
          <Testimonial {...testimonials[currentTestimonial]} />
        </div>

        {/* Botones de acción */}
        <div className="space-y-4 mb-8">
          <button 
            onClick={() => setCurrentScreen('login')}
            className="w-full bg-white text-[#2f4196] py-4 px-6 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1"
          >
            Iniciar Sesión
          </button>
          
          <button 
            onClick={() => setCurrentScreen('register')}
            className="w-full bg-white/10 backdrop-blur-sm text-white border-2 border-white/50 py-4 px-6 rounded-xl font-semibold hover:bg-white/20 transition-all duration-300"
          >
            Registrarse
          </button>

          {/* Botones de redes sociales */}
          <div className="flex justify-center space-x-4 mt-4">
            <button className="bg-white/90 p-3 rounded-lg hover:bg-white transition-colors duration-300">
              <img src="/api/placeholder/24/24" alt="Google" className="w-6 h-6" />
            </button>
            <button className="bg-white/90 p-3 rounded-lg hover:bg-white transition-colors duration-300">
              <img src="/api/placeholder/24/24" alt="Facebook" className="w-6 h-6" />
            </button>
            <button className="bg-white/90 p-3 rounded-lg hover:bg-white transition-colors duration-300">
              <img src="/api/placeholder/24/24" alt="Apple" className="w-6 h-6" />
            </button>
          </div>
        </div>
      </div>
    </div>
  );
};

export default MensajeriaExpressApp;
